using UnityEngine;

public class tryingArray : MonoBehaviour
{
    int[] numbers = { 10, 20, 30, 40, 50 };
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log(numbers[0]);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
